<UserControl x:Class="RecipeOrganiser.Views.AddIngredientView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RecipeOrganiser.Views"
			 xmlns:controls="clr-namespace:RecipeOrganiser.Resources.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

	<WrapPanel Margin="5">
		<TextBlock Style="{StaticResource FormText}" Margin="0,0,10,0">Name</TextBlock>
		<ComboBox ItemsSource="{Binding Categories}" IsEditable="True" StaysOpenOnEdit="True" Width="200" VerticalAlignment="Top" Margin="0,0,20,0"/>
		<TextBlock  Style="{StaticResource FormText}" Margin="0,0,10,0">Quantity</TextBlock>
		<controls:NumericUpDownTextBox Value="{Binding Value}" Margin="0,0,20,0"/>
		<TextBlock  Style="{StaticResource FormText}"  Margin="0,0,10,0">Weight</TextBlock>
		<controls:NumericUpDownTextBox Value="{Binding Value}" ValueType="Decimal" Margin="0,0,20,0"/>
		<ComboBox ItemsSource="{Binding Categories}" IsEditable="True" StaysOpenOnEdit="True" Width="80" VerticalAlignment="Top" Margin="0,0,20,0"/>
	</WrapPanel>

</UserControl>
